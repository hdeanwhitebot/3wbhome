{
  "name": "3wbhome",
  "private": true,
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "dev:resident": "turbo dev --filter=resident",
    "dev:vendor": "turbo dev --filter=vendor"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
